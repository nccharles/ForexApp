import EStyleSheet from 'react-native-extended-stylesheet';

export default EStyleSheet.create({
  smallText: {
    color: 'white',
    textAlign: 'center',
    textAlign: 'center',
    fontSize: 18,
    fontWeight: 600,
    paddingTop: 20,
  },
});
